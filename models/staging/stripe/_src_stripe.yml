sources:
  - name: stripe_raw
    description: Staging model for the Stripe payments table.
    database: raw
    schema: stripe
    tables:
      - name: payments
        config:
          loaded_at_field: _etl_timestamp
          freshness:
            warn_after:  {count : 24, period: hour}
            error_after: {count: 48, period: hour}